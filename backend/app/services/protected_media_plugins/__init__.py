"""Plugin package for protected media providers.

Each module in this package should define either:
- a module-level variable `provider` that implements ProtectedMediaProvider, or
- a function `get_provider()` returning such an instance.
"""
